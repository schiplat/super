# Optional — only for maintainers running `make fetch-keys` to refresh
# committed common/keys/*.public.key from Manager. Not needed for make build.
# Create an API token with `products.read` (Manager Settings → API tokens).

SUPER_MANAGER_BASE=http://127.0.0.1:8787
SUPER_MANAGER_PATH_PREFIX=pi
SUPER_PRODUCT_ID=super-pro
SUPER_MANAGER_TOKEN=
